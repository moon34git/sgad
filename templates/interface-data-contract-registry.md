# Interface / Data Contract Registry

Story 경계를 넘어 함수 시그니처·데이터 계약이 조용히 바뀌는 것을 막기 위한 문서.
`sgad` 스킬의 Stage 3 Step 6(Registry 업데이트)에서 갱신한다.

**상태 흐름:** `Proposed`(Seed 작성 시) → `Active`(Story 완료 시) → `Changed`(계약 변경 시,
영향받는 Consumer Story 명시) → `Deprecated`(사용 중단 시)

---

<!-- 아래를 항목마다 복사해서 채운다 -->

### IF-001

| 항목 | 값 |
|---|---|
| Module | |
| Function | |
| Signature | `func(arg: type) -> ReturnType` |
| Returns | |
| Producer | Story |
| Consumers | |
| Status | Proposed |
| Changed | — |
